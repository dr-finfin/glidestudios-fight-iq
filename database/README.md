# Database

The active control-plane schema lives in `schema.sql`.

Run it once in the Supabase SQL Editor for the GlideStudios project.

`migrations/001_n8n_schema.sql` exists only for the legacy n8n experiment and is not required by the Cloudflare architecture.
